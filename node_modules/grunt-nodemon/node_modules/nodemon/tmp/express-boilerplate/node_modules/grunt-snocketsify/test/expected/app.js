(function() {
  var SecretDependancy;

  SecretDependancy = (function() {
    function SecretDependancy() {
      console.warn('I should be kept secret!');
    }

    return SecretDependancy;

  })();

}).call(this);

var _dependancy = function() {
  return 'ok';
};

window.dependancy = _dependancy();


var Test = function() {
  if (dependancy === 'ok' && typeof SecretDependancy === 'undefined') {
    this.status = 'passing';
  }
  else {
    this.status = 'failing';
  }
};

(function() {
  console.warn("Test status: " + (new Test()).status);

}).call(this);
